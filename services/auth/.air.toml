# services/auth/.air.toml
root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o main ./cmd"
  bin = "main"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]
  log = "stdout"
  delay = 1000

[log]
  time = true

[env]
  APP_ENV = "development"
